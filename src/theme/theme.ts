import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  colors: {
    primary: "#743BD2", // purple
    primary100: "rgba(116, 59, 210, 0.11)", // light puple
    black: "#373351", // dark blue
    white: "#FAFAFA", // white
    grey: "#CAD1DC", // grey
  },
};

export default theme;
